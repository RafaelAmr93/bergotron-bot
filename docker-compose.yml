services:
  discord-bot:
    build: .
    container_name: bergotron
    restart: unless-stopped
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - TMDB_API_KEY=${TMDB_API_KEY}
      - PYTHONUNBUFFERED=1
    volumes:
      - ./data:/app/data     